apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    basicAuth: false
    jsonData:
      httpMethod: GET
      scrapeInterval: 15s
      queryTimeout: 60s
      timeInterval: 15s
      
  - name: Deployment Metrics External
    type: prometheus  
    access: proxy
    url: http://51.159.99.160:3004/metrics
    isDefault: false
    editable: true
    basicAuth: false
    jsonData:
      httpMethod: GET
      scrapeInterval: 30s
      queryTimeout: 30s
      timeInterval: 30s